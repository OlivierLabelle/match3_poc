package com.example.match3poc.ui

import androidx.compose.runtime.Composable
import androidx.compose.ui.unit.dp
import com.example.match3poc.domain.GridTileMovement
import androidx.compose.ui.Modifier

private val GRID_TILE_RADIUS = 4.dp

//Render the grid
@Composable
fun GameGrid(
    modifier: Modifier = Modifier,
    gridTileMovement: List<GridTileMovement>,
    moveCount: Int,
) {

}
